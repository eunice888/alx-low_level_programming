#include "main.h"

/**
 * add two integers and returns the result
 * u-integer to be added to v
 * v-integer to be added to u
 * Return: sum of u and v
 */
int add(int u, int v)
{
	int w;

	w = u + v;

	return (w);
}
