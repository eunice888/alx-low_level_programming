#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int u;

	for (u = 0; u < 10; u++)
	{
		printf("%d", u);
	}
	putchar('\n');
	return (0);
} 
