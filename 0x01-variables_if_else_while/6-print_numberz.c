#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int u;

	for (u = 48; u < 58; u++)
	{
		putchar(u);
	}
	putchar('\n');
	return (0);
} 
